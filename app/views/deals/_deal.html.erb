<div class="px-4 py-3 bg-white">
  <div class="flex flex-col justify-between">
    <div class="flex justify-between">
      <h2 class="text-neutral-600"><%= deal.name %></h2>
      <div class="text-neutral-900 font-semibold"><%= number_to_currency deal.amount %></div>
    </div>
    <small class="text-neutral-400"><%= deal.created_at.strftime("%d %b %Y") %></small>
  </div>
</div>
